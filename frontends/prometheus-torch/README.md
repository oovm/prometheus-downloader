# @doki-land/prometheus-torch

Node runtime used by `@doki-land/prometheus` to evaluate platform JavaScript and instantiate WebAssembly.

Install the product package (or the skill) rather than this runtime alone:

> Install `@doki-land/prometheus-skills` and finish the Prometheus setup and configuration for this environment.

Use only with content you have the right to download. Stored credentials belong to you.
