# @doki-land/prometheus-plugin

Contract and loader for Prometheus plugins (`@doki-land/prometheus-plugin-*`).

A plugin package exports `id`, `matches(url)`, and `inspect(url, ctx)`. The product runtime loads these modules; transfer still happens in the native engine.

Use only with content you have the right to download.
