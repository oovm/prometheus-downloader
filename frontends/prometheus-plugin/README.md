# @doki-land/prometheus-plugin

Contract and loader for Prometheus strategy plugins (`@doki-land/prometheus-strategy-*`).

A strategy package exports `id`, `matches(url)`, and `inspect(url, ctx)`. The product runtime loads these modules; transfer still happens in the native engine.

Use only with content you have the right to download.
